# Mamao--backend

## How to run?

`npm install`

then

in first terminal:

`npm run db:start`

in second terminal:

`npm run db:migrate`

after migrate, run:

`npm run start:dev`

Now you are ready to work with API.
